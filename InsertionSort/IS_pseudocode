function InsertionSort(A, N)
  for 1 <= j <= N-1 do
    ins = A[j]
    i = j-1
    while(i >= 0 and ins < A[i])
      A[i+1] = A[i]
      i = i-1
    end while
    A[i+1] = ins
  end for
end function
